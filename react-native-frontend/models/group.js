class Group {
	constructor(id, ownerId, title, people, users) {
		this.id = id
		this.ownerId = ownerId
		this.title = title
		this.people = people
		this.users = users       
	}
}

export default Group